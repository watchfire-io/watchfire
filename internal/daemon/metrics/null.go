package metrics

// null.go — explicit re-export so consumers grepping for the per-backend
// file shape find one. The actual implementation lives in parser.go to
// keep the interface and its trivial fallback co-located.
